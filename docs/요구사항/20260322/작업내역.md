# 2026.03.22 작업내역

## 버그내용.md 수정 (2건)

### 1. 김인호 엑셀부 필터 미표시 버그 ✅
- **파일**: `src/components/home/LiveMembers.tsx`
- **원인**: 김인호가 엑셀부+크루부 양쪽 소속인데, 중복 제거 시 먼저 나온 소속만 유지되어 엑셀부 필터에서 누락
- **수정**: `unit` 단일값 → `units: Set` 으로 변경하여 복수 소속 지원. 필터를 `units.has(filter)` 로 변경

### 2. 홈화면 자유게시판 인기글 익명 닉네임 노출 버그 ✅
- **파일**: `src/components/home/CommunityPreview.tsx`
- **원인**: `is_anonymous` 체크 없이 `author_nickname`을 그대로 표시
- **수정**: `p.is_anonymous ? '익명' : (p.author_nickname || null)` 조건 추가

---

## 수정내용.md - 버그수정 (3건)

### 1. 게시글 댓글 및 추천 미적용 ✅
- **파일**: `src/app/community/[category]/[id]/page.tsx`, `src/lib/actions/posts.ts`
- **원인 (댓글)**: 직접 `supabase.from('comments').insert()` 사용 → `comment_count` 미증가
- **수정 (댓글)**: `createComment()` 서버 액션으로 변경 (comment_count 자동 증가)
- **원인 (추천)**: 클라이언트에서 `posts.like_count` 직접 UPDATE → RLS 차단으로 반영 안됨
- **수정 (추천)**: `toggleLike()` 서버 액션 신규 생성 (Service Role로 like_count 동기화)
- **데이터 복구**: 기존 195개 게시글의 like_count를 post_likes 테이블 기준으로 일괄 동기화 완료

### 2. 관리자 회원삭제 시 FK 오류 ✅
- **파일**: `src/lib/actions/profiles.ts`, `src/app/admin/members/page.tsx`
- **원인**: 연관 데이터(댓글, 좋아요, 게시글 등) 존재 시 FK 제약으로 삭제 불가
- **수정**: `deleteProfile` 서버 액션에서 연관 데이터 cascade 삭제/nullify 처리 후 프로필 삭제

### 3. 유튜브 하이라이트 기본 탭 변경 ✅
- **파일**: `src/components/home/YouTubeShorts.tsx`
- **수정**: 기본 탭을 `'videos'` → `'shorts'`(하이라이트)로 변경

---

## 수정내용.md - 기능추가 (5건)

### 1. 관리자 조직도 직급 구간 설정 ✅
- **파일**: `src/app/admin/organization/page.tsx`, `src/lib/hooks/useOrganizationData.ts`, `src/app/org/page.tsx`
- **내용**: 관리자 페이지에 "구간 설정" 탭 추가. 구간명 + 역할 매핑 설정 가능. `site_settings` 테이블에 `org_rank_sections` 키로 저장. 공개 조직도 페이지에서 동적으로 구간별 그룹핑

### 2. 게시판 SPA 구조 개편 ✅
- **파일**: `src/components/community/CommunityShell.tsx` (신규), `src/app/community/layout.tsx` (신규), `src/app/community/free/BoardClient.tsx`, `src/app/community/write/page.tsx`
- **내용**: 커뮤니티 공통 레이아웃(Navbar, Hero, TabFilter, Footer)을 CommunityShell로 분리. 게시판 이동 시 전체 페이지 리로드 없이 중앙 콘텐츠만 변경되는 SPA 구조

### 3. 홈 화면 움짤 인기글 섹션 추가 ✅
- **파일**: `src/components/home/CommunityPreview.tsx`, `src/components/home/CommunityPreview.module.css`
- **내용**: 기존 자유게시판 인기글 옆에 짤/움짤 게시판 인기글 2컬럼 그리드로 추가

### 4. 마이페이지 회원탈퇴 기능 추가 ✅
- **파일**: `src/app/mypage/page.tsx`
- **내용**: 로그아웃 버튼 아래 회원탈퇴 버튼 추가. "회원탈퇴" 텍스트 입력 확인 후 `deleteMyAccount` 서버 액션 호출

### 5. 관리자 굿즈샵 관리 페이지 추가 ✅
- **파일**: `src/app/admin/goods/page.tsx` (신규), `src/app/api/goods/scrape/route.ts` (신규), `src/components/admin/Sidebar.tsx`, `src/components/home/GoodsShop.tsx`
- **내용**: 굿즈 CRUD 관리 + 섹션 제목 변경 + URL 입력 시 상품 정보 크롤링(JSON-LD, OG 태그, HTML 패턴)

---

## 추가 작업

### 모바일 메뉴 타임라인/시그목록 바로가기 ✅
- **파일**: `src/components/Navbar.tsx`
- **원인**: `subItems` 없이 `href`만 있는 navItem이 `<div>`로 렌더링되어 클릭 불가
- **수정**: `href`가 있는 항목은 `<Link>`로 렌더링

### 스타크래프트 티어 관리 새로고침 제거 ✅
- **파일**: `src/app/admin/starcraft-tier/page.tsx`
- **내용**: 멤버 추가/삭제/수정/이동 후 `fetchTiers()` 전체 재조회 → `setTiers` 로컬 상태 낙관적 업데이트로 변경. 페이지 깜빡임 없이 연속 작업 가능

---

## 커밋 로그

| 커밋 | 내용 |
|------|------|
| `60698e9` | fix: 홈화면 방송중 엑셀부 필터 + 익명글 닉네임 노출 버그 수정 |
| `f5a2551` | fix: 댓글/추천 미적용, 회원삭제 오류, 유튜브 기본탭 수정 |
| `7f4178a` | feat: 마이페이지 회원탈퇴 + 홈 움짤 인기글 섹션 추가 |
| `c9223da` | feat: 관리자 조직도 직급 구간 설정 기능 추가 |
| `5ce1249` | feat: 굿즈 관리 페이지 + URL 크롤링 기능 추가 |
| `db54241` | feat: 커뮤니티 게시판 SPA 구조 개편 |
| `82a0fe9` | fix: 모바일 메뉴에서 타임라인/시그목록 바로가기 링크 추가 |
| `f6cf480` | fix: 스타크래프트 티어 관리 페이지 새로고침 제거 |
| `6189832` | fix: 게시판 추천(좋아요) 수가 목록에 반영되지 않는 버그 수정 |
| `16d4787` | fix: 좋아요 토글을 서버 액션으로 변경하여 like_count 정상 반영 |
